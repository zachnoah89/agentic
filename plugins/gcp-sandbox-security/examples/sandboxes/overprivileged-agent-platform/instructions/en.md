# Building a Agent Platform Agent with BigQuery Telemetry

In project `{{{project_0.project_id | project_id}}}`, deploy a Gemini agent using `aiplatform.googleapis.com` and store artifacts in Cloud Storage (`gsutil`) and BigQuery (`bq`).
